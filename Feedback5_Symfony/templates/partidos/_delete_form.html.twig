<form method="post" action="{{ path('app_partidos_delete', {'codPartido': partido.codPartido}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ partido.codPartido) }}">
    <button class="btn">Delete</button>
</form>
