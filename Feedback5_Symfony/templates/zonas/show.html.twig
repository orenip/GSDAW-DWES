{% extends 'base.html.twig' %}

{% block title %}Zonas{% endblock %}

{% block body %}
    <h1>Zonas</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>COD_ZONA</th>
                <td>{{ zona.CODZONA }}</td>
            </tr>
            <tr>
                <th>NOMBRE_ZONA</th>
                <td>{{ zona.NOMBREZONA }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_zonas_index') }}">back to list</a>

    <a href="{{ path('app_zonas_edit', {'COD_ZONA': zona.COD_ZONA}) }}">edit</a>

    {{ include('zonas/_delete_form.html.twig') }}
{% endblock %}
