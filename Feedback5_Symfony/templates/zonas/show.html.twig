<!DOCTYPE html>

<title>Zonas</title>

{% block body %}
    <h1>Zonas</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>CodZona</th>
                <td>{{ zona.codZona }}</td>
            </tr>
            <tr>
                <th>NombreZona</th>
                <td>{{ zona.nombreZona }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_zonas_index') }}">back to list</a>

    <a href="{{ path('app_zonas_edit', {'codZona': zona.codZona}) }}">edit</a>

    {{ include('zonas/_delete_form.html.twig') }}
{% endblock %}
