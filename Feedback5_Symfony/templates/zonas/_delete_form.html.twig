<form method="post" action="{{ path('app_zonas_delete', {'COD_ZONA': zona.COD_ZONA}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ zona.COD_ZONA) }}">
    <button class="btn">Delete</button>
</form>
