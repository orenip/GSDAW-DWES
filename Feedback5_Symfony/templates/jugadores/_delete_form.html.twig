<form method="post" action="{{ path('app_jugadores_delete', {'codJugador': jugadore.codJugador}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ jugadore.codJugador) }}">
    <button class="btn">Delete</button>
</form>
