<form method="post" action="{{ path('app_zonas_delete', {'id': zona.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ zona.id) }}">
    <button class="btn">Delete</button>
</form>
